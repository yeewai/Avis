- if !@project.nil?
  - @plink = @project
  ul#breadcrumbs
    - while @plink.parent_id != 0 
      li
        = link_to @plink.parent.name, @plink.parent
        - @plink = @plink.parent
    li#current
      = link_to @project.name, @project